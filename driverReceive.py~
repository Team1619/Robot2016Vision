import socket

sock = socket.socket()
sock.bind(('192.168.1.170', 5801))

print 'listening'
sock.listen(1)

conn, addr = sock.accept()
print 'Accepted', str(conn), str(addr)

conn.close()
sock.close()
